# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Flutter Web page to docs
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git config --global user.name "$GITHUB_ACTOR" 
      - run: git fetch
      - run: git checkout web-deploy
      - run: git merge main --allow-unrelated-histories
      - uses: subosito/flutter-action@v1
      - run: flutter build web
      - run: sed -i 's+<base href="/">+<base href="./">+g' build/web/index.html
      - run: rsync -a build/web/* docs/
      - run: flutter clean
      - name: Making commit
        run: |
          git add . \
          git commit -m "Commit from action for web deploy" \
          git push
